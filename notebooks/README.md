# Notebooks

Suggested notebook workflow:

1. `01_eda.ipynb` for raw dataset inspection.
2. `02_preprocessing.ipynb` for feature selection and scaling experiments.
3. `03_model_experiments.ipynb` for model comparisons.

The production pipeline lives in `src/` and `scripts/`.
